{
  "type": "object",
  "properties": {
    "content": {
      "type": "string",
      "description": "Conversation response from the AI describing the project analysis and plan."
    },
    "managers": {
      "type": "array",
      "description": "List of manager nodes to handle parts of the project.",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Manager node name describing its role (e.g., 'DataCollector', 'ModelTrainer')."
          },
          "job": {
            "type": "string",
            "description": "Detailed description of the manager's responsibilities, tasks, and goals."
          }
        },
        "required": ["name", "job"]
      }
    }
  },
  "required": ["content", "managers"]
}
