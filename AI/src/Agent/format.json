{
  "type": "object",
  "properties": {
    "content": {
      "type": "string",
      "description": "Conversation response from the AI describing the work done by the agent."
    },
    "files": {
      "type": "array",
      "description": "List of files produced by the agent.",
      "items": {
        "type": "object",
        "properties": {
          "filename": {
            "type": "string",
            "description": "Name of the file including extension."
          },
          "content": {
            "type": "string",
            "description": "The full content of the file."
          }
        },
        "required": ["filename", "content"]
      }
    }
  },
  "required": ["content", "files"]
}
