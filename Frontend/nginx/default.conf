server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # ✅ Safe CSP options:
  add_header Content-Security-Policy "script-src 'self' https://static.cloudflareinsights.com https://apis.google.com https://api.nodpt.com https://nodpt.com https://*.nodpt.com https://*.cloudflare.com https://static.cloudflareinsights.com https://*.googleapis.com https://*.firebaseapp.com https://*.firebase.com 'unsafe-inline';" always;

  # Optional extras
  add_header X-Content-Type-Options "nosniff" always;
  add_header Referrer-Policy "no-referrer" always;
}
